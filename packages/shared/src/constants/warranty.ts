/**
 * Flat post-job warranty window applied to every completed job — docx Finance
 * section: "a dedicated tab for post-job warranty (e.g., 7-day or 12-month
 * coverage on specific repairs)". Per-category coverage windows (e.g. 12
 * months on electrical work vs 7 days on cleaning) need a real policy table
 * this MVP doesn't have yet; this default keeps the feature honest rather
 * than fabricating per-category rules with no backing data.
 */
export const DEFAULT_WARRANTY_WINDOW_DAYS = 7;

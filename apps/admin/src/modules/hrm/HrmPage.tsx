/** Member directory, attendance, dispatch calendar — see ARCHITECTURE.md §3. */
export function HrmPage() {
  return (
    <section>
      <h2>HRM</h2>
      <p>Member directory and the drag-and-drop dispatch calendar land here.</p>
    </section>
  );
}

/**
 * Fund dashboard, pay editor, dispute resolution — see ARCHITECTURE.md §3.
 * TODO: restrict to finance_head/ceo roles once auth (apps/api `auth` module) exists — ARCHITECTURE.md §7.
 */
export function FinancePage() {
  return (
    <section>
      <h2>Finance</h2>
      <p>Fund dashboard and dispute resolution land here.</p>
    </section>
  );
}

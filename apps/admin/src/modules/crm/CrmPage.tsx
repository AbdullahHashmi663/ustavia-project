/** Active/completed jobs, verification queue, SOS monitor — see ARCHITECTURE.md §3. */
export function CrmPage() {
  return (
    <section>
      <h2>CRM</h2>
      <p>Job monitoring and verification Kanban land here.</p>
    </section>
  );
}
